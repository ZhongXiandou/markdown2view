fix: 修复多项A4文档分页排版及Markdown表格渲染核心Bug

1. 修复表格跨页分页估算算法导致的底部“吞行”和溢出裁切问题：
   - 修正 `heightRatio` 测算口径，排除外部 margin 干扰；
   - 确保跨页时表头重新渲染产生的高度被准确计入单页总计；
   - 引入动态列宽智能估算（模拟 `table-layout: auto`）大幅提升文字折行高度估算精度；
   - 增加 30px 分页边界安全缓冲带以及防极窄空间起步机制。
2. 修复排版算法导致的页面底部大面积“异常留白”问题：
   - 废除粗暴的 `HEADING_NEAR_BOTTOM_RATIO`（固定 78% 阈值）防孤立逻辑；
   - 引入智能前瞻（Look-ahead）算法，让标题向后探测绑定正文块的高度，动态决定是否跨页，彻底消灭无效留白。
3. 修复含有空单元格（如 `| |`）的 Markdown 表格边框与内容严重错位问题：
   - 移除原解析逻辑中滥用的 `.filter(Boolean)`；
   - 重构行切割逻辑为精准定点剥离首尾 `|` 后保留中间空字符串，确保单元格数量 `<th>` 与 `<td>` 严格一致。
4. 知识沉淀更新：
   - 将以上隐蔽的算法陷阱及破局思路总结沉淀至 `docs/开发经验与偏好记录.md`，巩固排版及隐藏DOM测量标准。
