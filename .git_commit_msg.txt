fix: 修正指引弹窗会话控制，确保刷新页面时重新弹出

- 原逻辑在用户点击“开始使用”时将 'session' 标记写入 localStorage，导致刷新页面也无法再次弹起；
- 改为使用组件外的内存 Set (sessionSeenKeys) 进行会话范围的关闭记录，确保在单次页面加载期间切换模式不重复弹出，而当用户手动刷新页面时重置内存状态，使引导弹窗重新展出；
- 只有当用户显式点击“不再显示”时，才在 localStorage 记录永久关闭状态（值为 '1'）。
