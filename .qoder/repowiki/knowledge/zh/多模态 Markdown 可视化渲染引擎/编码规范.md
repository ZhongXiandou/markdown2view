- 组件采用 `{Name}_{Type}{Variant}{Version}` 命名规范（如 `Title_DA01`），并在 `engine/editor-components/index.ts` 中统一注册映射。
- 渲染引擎函数（如 `parseMarkdown`）接收 `ThemeColors` 对象作为参数，确保生成的 HTML 能够动态适配用户选择的主题色。
- 所有自定义 Markdown 块级元素均通过正则匹配特定标签（如 `<steps>`、`<cta>`）并调用对应的 `render` 方法转换为带内联样式的 HTML 片段。