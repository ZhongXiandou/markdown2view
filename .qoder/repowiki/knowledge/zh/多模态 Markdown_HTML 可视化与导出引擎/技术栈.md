- **状态管理**：Zustand with `persist` middleware for local storage synchronization.
- **样式方案**：Tailwind CSS for utility-first styling, combined with inline styles in the engine for export compatibility.
- **构建工具**：Vite (inferred from `vite-env.d.ts` and `main.tsx`).
- **导出依赖**：Client-side image capture (likely `html-to-image` or similar via `exportImage.ts`) and JSZip for multi-page exports.