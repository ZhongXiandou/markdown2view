- 采用“设计资产 (Design Assets)”命名规范，引擎组件以 `_DAxx` 后缀标识不同视觉变体（如 `Title_DA01.ts`）。
- 在 `store.ts` 中实施“脏数据标记 (Dirty Flag)”机制，区分用户手动编辑内容与系统示例内容，防止版本更新时误覆盖用户成果。
- 编辑器同步采用“防抖回写 + 外部版本信号”策略，解决 React 受控组件在 IME 输入或高频打字时的竞态丢字问题。
- 所有 UI 交互反馈统一通过 `onToast` 回调触发全局 `Toast` 组件，保持提示逻辑与业务组件解耦。