- **分层架构协同**：根目录 `src/` 提供底层 Markdown 解析与模式切换核心（markdown2view），`html-anything/next` 作为上层宿主应用，通过 API 路由调用底层能力并集成 AI Agent 工作流。
- **技能标准化注入**：`.agent/skills` 目录定义了跨模块通用的设计规范（如 Vercel 最佳实践、Guizang PPT 模板），作为共享知识资产被编辑器与生成引擎共同引用，确保输出视觉的一致性。
- **统一工程化底座**：采用 pnpm workspace 管理多包依赖，通过顶层 `vite.config.ts` 与 `tsconfig.json` 约束 TypeScript 编译行为与前端构建流程，实现开发环境的无缝衔接。